Parked marketing provider wrappers live here when they are not currently owned by an active skill.

They stay available for later reuse without cluttering the active provider set in `skills/_providers/marketing/`.

Current parked wrappers:
- `buffer.mjs` - publishing remains owned by `post-scheduler`
- `rewardful.mjs` - no active affiliate-focused marketing skill owns it yet
- `tolt.mjs` - no active affiliate-focused marketing skill owns it yet
