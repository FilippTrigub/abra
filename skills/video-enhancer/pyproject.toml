[project]
name = "video-enhancer"
version = "0.1.0"
description = "Batch video enhancement: sharpening, colour grading, audio normalisation"
requires-python = ">=3.12"
dependencies = [
    "ffmpeg-python",
    "ffmpeg-normalize",
]
