[project]
name = "giphy"
version = "0.1.0"
description = "Overlay animated GIF stickers from GIPHY onto videos"
requires-python = ">=3.10,<3.14"
dependencies = [
    "pillow>=10.0",
    "requests>=2.31",
]

[tool.uv]
dev-dependencies = []
