[project]
name = "pixabay"
version = "0.1.0"
description = "Overlay Pixabay royalty-free images and video clips onto videos"
requires-python = ">=3.10,<3.14"
dependencies = [
    "requests>=2.31",
]

[tool.uv]
dev-dependencies = []
