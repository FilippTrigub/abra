[project]
name = "freesound"
version = "0.1.0"
description = "Mix Freesound sound effects into videos with optional GIF overlays"
requires-python = ">=3.10,<3.14"
dependencies = [
    "pillow>=10.0",
    "requests>=2.31",
]

[tool.uv]
dev-dependencies = []
