[project]
name = "remotion-video"
version = "0.1.0"
description = "Contract scaffold and skill-local Python orchestration runtime for remotion-video"
requires-python = ">=3.12"
dependencies = [
    "jsonschema==4.25.1",
]

[dependency-groups]
dev = [
    "pytest==8.4.2",
    "ruff==0.14.0",
]

[tool.uv]
package = false
