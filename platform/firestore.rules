rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️  INITIAL DEV RULES — permissive for local testing only.
    //  TODO: Harden in Task 14 (production-ready security rules).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
